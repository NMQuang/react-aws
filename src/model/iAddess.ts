export interface IAddressSelect {
  label: string;
  value: string;
  latitude: string;
  longitude: string;
}

export interface IAddressResponse {
  postCode: string;
  allAddress: string;
  latitude: string;
  longitude: string;
}
